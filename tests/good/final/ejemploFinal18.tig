let
  type b = array of a
  type a = c
  type c = {code : int, name : b}
  var x:b := b [2] of nil
  var z:b := b [44] of nil
  var y:c := c {code = 777, name = x}
in
  x[0] := c{code = -66, name = z};
  x[1] := c{code = -99, name = z};
  y.code + y.name[0].code
end
